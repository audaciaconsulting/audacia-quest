@foreach (var asset in Context.Assets)
{
    <img @ref="asset.Ref" src="@asset.Renderer.ImageSource" @onload="asset.ContentLoaded" />
}

@code {
    [Parameter]
    public BlazorGameContext Context { get; set; }
}
